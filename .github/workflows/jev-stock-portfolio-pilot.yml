name: jev-stock-portfolio-pilot-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy paid Jev workflow is disabled."
          echo "Use the manually budgeted safe-jev-direction-probe workflow instead."
