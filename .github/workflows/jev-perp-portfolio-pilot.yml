name: jev-perp-portfolio-pilot

on:
  workflow_dispatch:
    inputs:
      symbols:
        description: Comma-separated Binance USD-M perpetual symbols
        required: true
        default: "BTCUSDT,ETHUSDT,SOLUSDT"
      interval:
        description: Binance kline interval
        required: true
        default: "15m"
      start:
        description: Historical start date
        required: true
        default: "2025-01-01"
      end:
        description: Historical end date
        required: true
        default: "2025-02-01"
      horizons:
        description: Forecast horizons in bars
        required: true
        default: "6,12"
      profiles:
        description: Jev input profiles
        required: true
        default: "technical,path,cross,full"
      decision_every:
        description: Evaluate every N synchronized bars
        required: true
        default: "8"
      top_n:
        description: Initial maximum simultaneously selected contracts
        required: true
        default: "2"
      max_asset:
        description: Initial maximum absolute exposure per contract
        required: true
        default: "0.5"
      spread_bps:
        description: Modeled quoted spread
        required: true
        default: "4"
      fee_bps:
        description: Modeled fee per side
        required: true
        default: "4"
      slippage_bps:
        description: Modeled slippage per side
        required: true
        default: "1"
      concurrency:
        description: Maximum concurrent Jev API calls
        required: true
        default: "4"
      max_new_evals:
        description: Hard cap across all fresh Jev calls
        required: true
        default: "8000"
      probe_run_id:
        description: Optional completed jev-perp-portfolio-probe run ID whose exact universe and cache should be reused
        required: false
        default: ""

permissions:
  contents: read
  actions: read

jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      TYPESAFE_AI_API_KEY: ${{ secrets.TYPESAFE_AI_API_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/replay-lab
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Require TypeSafe secret
        run: |
          if [ -z "$TYPESAFE_AI_API_KEY" ]; then
            echo "TYPESAFE_AI_API_KEY repository secret is not configured"
            exit 1
          fi
      - name: Download perpetual universe
        if: ${{ inputs.probe_run_id == '' }}
        run: |
          bun run scripts/download-binance-perp-universe.ts \
            --symbols="${{ inputs.symbols }}" \
            --interval="${{ inputs.interval }}" \
            --start="${{ inputs.start }}" \
            --end="${{ inputs.end }}" \
            --spread-bps="${{ inputs.spread_bps }}" \
            --out-dir=data/market/perp-universe \
            --manifest=data/market/perp-universe/universe.json
      - name: Download optional exact perp probe artifact
        if: ${{ inputs.probe_run_id != '' }}
        uses: actions/download-artifact@v4
        with:
          pattern: jev-perp-portfolio-probe-${{ inputs.probe_run_id }}
          path: probe
          merge-multiple: true
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ inputs.probe_run_id }}
      - name: Reuse verified perp universe and probe cache
        if: ${{ inputs.probe_run_id != '' }}
        run: |
          test -s probe/market/perp-universe/universe.json
          test -s probe/perp-portfolio-probe-summary.json
          test -s probe/perp-portfolio-probe-cache.jsonl
          jq -e \
            --arg symbols "${{ inputs.symbols }}" \
            --arg interval "${{ inputs.interval }}" \
            --arg start "${{ inputs.start }}" \
            --arg end "${{ inputs.end }}" \
            '.requestedSymbols == ($symbols | split(",")) and .interval == $interval and .start == $start and .end == $end' \
            probe/market/perp-universe/universe.json >/dev/null
          mkdir -p data/market
          cp -R probe/market/perp-universe data/market/perp-universe
          ACTUAL="$(bun -e 'import { fingerprintUniverse } from "./research/universe"; console.log(fingerprintUniverse("data/market/perp-universe/universe.json").combinedSha256)')"
          PROBE="$(jq -r '.fingerprint.combinedSha256' probe/perp-portfolio-probe-summary.json)"
          if [ "$ACTUAL" != "$PROBE" ]; then
            echo "Perp probe universe fingerprint does not match copied universe"
            exit 1
          fi
          cp probe/perp-portfolio-probe-cache.jsonl data/perp-portfolio-pilot-cache.jsonl
      - name: Run development-only Jev perpetual portfolio pilot
        run: |
          bun run research/portfolio-pilot.ts data/market/perp-universe/universe.json \
            --model=jev \
            --require-complete-grid=true \
            --concurrency="${{ inputs.concurrency }}" \
            --horizons="${{ inputs.horizons }}" \
            --profiles="${{ inputs.profiles }}" \
            --decision-every="${{ inputs.decision_every }}" \
            --top-n="${{ inputs.top_n }}" \
            --max-gross=1 \
            --max-asset="${{ inputs.max_asset }}" \
            --spread-bps="${{ inputs.spread_bps }}" \
            --fee-bps="${{ inputs.fee_bps }}" \
            --slippage-bps="${{ inputs.slippage_bps }}" \
            --allow-short=true \
            --max-new-evals="${{ inputs.max_new_evals }}" \
            --cache=data/perp-portfolio-pilot-cache.jsonl \
            --out=data/perp-portfolio-pilot-summary.json
      - name: Tune funding-aware shared-bankroll apparatus from cache
        run: |
          bun run research/tune-portfolio-apparatus.ts data/market/perp-universe/universe.json \
            --pilot=data/perp-portfolio-pilot-summary.json \
            --cache=data/perp-portfolio-pilot-cache.jsonl \
            --grid=full \
            --short-borrow-bps-day=0 \
            --out=data/perp-portfolio-apparatus-selection.json
      - name: Freeze only if validation qualification passes
        run: |
          bun run research/freeze-portfolio.ts data/market/perp-universe/universe.json \
            --selection=data/perp-portfolio-apparatus-selection.json \
            --short-borrow-bps-day=0 \
            --out=data/perp-portfolio-apparatus-freeze.json
      - name: Zero-spend perp sealed-test preflight
        if: ${{ success() }}
        run: |
          bun run research/portfolio-sealed-preflight.ts data/perp-portfolio-apparatus-freeze.json \
            --manifest=data/market/perp-universe/universe.json \
            --cache=data/perp-portfolio-pilot-cache.jsonl | tee data/perp-portfolio-sealed-preflight.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jev-perp-portfolio-pilot-${{ github.run_id }}
          path: |
            data/market/perp-universe/
            data/perp-portfolio-pilot-cache.jsonl
            data/perp-portfolio-pilot-summary.json
            data/perp-portfolio-apparatus-selection.json
            data/perp-portfolio-apparatus-freeze.json
            data/perp-portfolio-sealed-preflight.txt
          retention-days: 30
