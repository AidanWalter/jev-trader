name: jev-portfolio-fresh-window-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy paid Jev workflow is disabled."
          echo "Use safe-jev-direction-probe for any future paid Jev call."
